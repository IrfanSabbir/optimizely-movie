import { LinearProgress } from "@mui/material";

const Spinner = () => (
  <>
    <LinearProgress color="secondary" />
    <br />
    <LinearProgress color="secondary" />
    <br />
    <LinearProgress color="secondary" />
  </>
);

export default Spinner;
